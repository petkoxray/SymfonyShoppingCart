{% extends "@ShoppingCart/base.html.twig" %}

{% block body %}

    {% if is_granted('ROLE_ADMIN') %}
        <h1>Admin Panel</h1>
        <a href="{{ path('admin_users_all') }}" type="button" class="fa fa-users fa-lg btn btn-info btn-lg btn-block"> Edit Users</a>
        <hr>
    {% endif %}
        <h1>Editor Panel</h1>
    <a href="{{ path('admin_categories_all') }}" type="button" class="fa fa-window-restore fa-lg btn btn-info btn-lg btn-block"> Edit Categories</a>
    <button type="button" class="fa fa-briefcase fa-lg btn btn-info btn-lg btn-block"> Edit Products</button>

{% endblock %}