{% extends "@ShoppingCart/base.html.twig" %}

{% block body %}

    {{ form_start(add_form) }}

    {{ form_row(add_form.ip) }}

    <button class="btn btn-success" formnovalidate>Add ip to the blacklist <i
                class="fa fa-plus-circle" aria-hidden="true"></i>
    </button>

    {{ form_end(add_form) }}

{% endblock %}