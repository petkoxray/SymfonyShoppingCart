{% extends "@ShoppingCart/base.html.twig" %}

{% block body %}

    <div class="panel panel-default">
        <div class="panel-heading">
            <h3 class="panel-title">Profile details:</h3>
        </div>
        <div class="panel-body">
            <table class="table table-responsive">
                <tr>
                    <th>Email</th>
                    <td>{{ user.email }}</td>
                </tr>
                <tr>
                    <th>Name</th>
                    <td>{{ user.fullName }}</td>
                </tr>
                <tr>
                    <th>Roles</th>
                    <td>{{ user.roles|join(", ") }}</td>
                </tr>
                <tr>
                    <th>Money</th>
                    <td><strong>€{{ user.money }}</strong></td>
                </tr>
            </table>
            <a class="btn-info btn fa fa-thumbs-up" href="{{ path('user_profile_edit') }}">Edit</a>
        </div>
    </div>

{% endblock %}